/**
 * 인풋 아이콘 타입
 */
export enum inputIconType {
	OEPNEDEYEICON = 'OpenedEyeIcon',
}

/**
 * 모달 버튼 타입
 */
export enum ModalButtonType {
	ACTIVE = 'active',
	CANCLE = 'cancle',
}

/**
 * 인풋 벨리데이션 패턴 타입
 */
export enum InputPatternType {
	EMAIL = 'Email',
	PASSWORD = 'password',
	NAME = 'name',
}

/**
 * 테이블 셀 텍스트 포멧 타입
 */
export enum TextFormatType {
	FORMATTYPE = 'YYYY-MM-DD HH:mm:ss',
}
